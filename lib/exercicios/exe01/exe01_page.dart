import 'package:flutter/material.dart';

class Exe01Page extends StatelessWidget {
  const Exe01Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Hello World"),
      ),
    );
  }
}
